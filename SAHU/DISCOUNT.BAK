#include<stdio.h>
#include<conio.h>
void main()
{
	int per,dis=0,pay;
	clrscr();
	printf("enter perchases:\n ");
	scanf("%d",&per);
	if(per<1000)
		printf("DMC\n");
	else
		if(per<=5000)
			dis=per*.1;
		else
			if(per<=10000)
				dis=per*.2;
			else
				dis=per*.25;
	pay=per-dis;
	printf("payment=%d",pay);
	getch();
}