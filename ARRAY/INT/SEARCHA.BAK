#include<stdio.h>
#include<conio.h>
void main()
{
	int a[5],i,ser;
	clrscr();
	printf("Enter five no: ");
	for(i=0;i<5;i++)
		scanf("%d",&a[i]);
	printf("Enter no to search: ");
	scanf("%d",&ser);
	for(i=0;i<5;i++)
	{
		if(ser==a[i])
		{
			printf("%d found ",ser);
			break;
		}
	}
	if(i==5)
		printf("%d not found",ser);
}